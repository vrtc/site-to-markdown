# URL to Markdown API

Преобразуйте любой веб-контент в чистый Markdown, готовый для LLM, одним API-вызовом! Этот мощный сервис на FastAPI без проблем конвертирует веб-страницы, документы и мультимедийный контент в структурированный Markdown-формат, что делает его идеальным для AI/ML-пайплайнов, агрегации контента и обработки данных.

 

## 🚀 Ключевые возможности

- **Универсальная поддержка контента**: Конвертируйте веб-статьи, YouTube-видео, PDF, Office-документы и многое другое
- **Оптимизация для LLM**: Чистый, структурированный Markdown, идеально подходящий для обработки AI/ML
- **Работа с мультимедиа**: Извлекает метаданные из изображений, аудио и видеофайлов
- **Умная обработка**: OCR для изображений, транскрипция для аудио и интеллектуальное извлечение контента
- **Простая интеграция**: RESTful API с чёткой обработкой ошибок и кодами ответов

## Возможности

- Конвертация веб-страниц в чистый Markdown, оптимизированный для обработки LLM
- Поддержка различных типов контента, включая:
  - Веб-статьи и HTML-контент
  - YouTube-видео
  - PDF-документы
  - Презентации PowerPoint
  - Документы Word
  - Электронные таблицы Excel
  - Изображения (с EXIF-метаданными и OCR)
  - Аудиофайлы (с метаданными и транскрипцией)
  - Текстовые форматы (CSV, JSON, XML)
  - ZIP-архивы (обрабатывает содержимое)
- Автоматическая обработка протоколов URL
- Чистая обработка ошибок с соответствующими HTTP-кодами статусов

## Почему Markdown для LLM?

- **Структурированный формат**: Markdown предоставляет чистую, иерархическую структуру, которую LLM легко парсит и понимает
- **Единообразное представление**: Различные типы контента нормализуются в единый текстовый формат
- **Сохранение семантики**: Заголовки, списки и выделения сохраняются так, чтобы сохранить структуру документа
- **Снижение шума**: Удаляет ненужное форматирование и стилизацию, фокусируясь на контенте
- **Повышенная доступность**: Делает контент более доступным для текстового анализа и обработки естественного языка

## Установка

### Стандартная установка

1. Клонируйте репозиторий
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

### Развертывание с Docker

1. Использование Docker Compose (рекомендуется)

```bash
docker-compose up -d
```

Это соберёт и запустит сервис в фоновом режиме. API будет доступен по адресу `http://localhost:8000`

2. Использование Docker напрямую

```bash
# Сборка образа
docker build -t url-to-markdown .

# Запуск контейнера
docker run -d -p 8000:8000 url-to-markdown
```

## Использование

Запустите сервер:

```bash
uvicorn main:app --reload
```

API будет доступен по адресу `http://localhost:8000`

### Эндпоинт API

```
GET /{url}
```

URL должен быть URL-кодирован, если содержит специальные символы.

### Примеры использования

1. **Конвертация YouTube-видео**

    ```
    GET http://localhost:8000/www.youtube.com/watch?v=dQw4w9WgXcQ
    ```

    Вернёт название видео, описание и метаданные в формате Markdown.

2. **Конвертация PDF-документов**

    ```
    GET http://localhost:8000/https://pdfobject.com/pdf/sample.pdf
    ```

    Конвертирует содержимое PDF в читаемый Markdown-текст.

3. **Конвертация веб-статей**
    ```
    GET http://localhost:8000/https://dev.to/iw4p/scraping-tweets-without-twitter-api-and-free-5g9c
    ```
    Конвертирует содержимое статьи в чистый Markdown-формат.

4. **Конвертация с использованием полного URL**

    ```
    GET http://localhost:8000/https%3A%2F%2Fexample.com%2Fpage
    ```

    URL можно передавать как в закодированном, так и в незакодированном виде - код автоматически обрабатывает оба формата.

### Формат ответа

Успешный ответ: Plain text Markdown-контент

```markdown
# Название статьи

## Контент

[Содержимое статьи в формате Markdown]
```

### Коды ошибок

- `400`: Ошибка обработки URL
- `415`: Неподдерживаемый формат URL
- `500`: Внутренняя ошибка сервера
- `502`: Ошибка при получении URL (не удалось загрузить содержимое)
- `504`: Превышено время ожидания конвертации

## Разработка

Этот проект использует:

- FastAPI для веб-фреймворка
- MarkItDown для конвертации контента
- Python 3.11+


